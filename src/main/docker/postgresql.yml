version: '2'
services:
  afiliados-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/afiliados/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=afiliados
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
